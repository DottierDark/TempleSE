import EditableMap from '../../Components/Map';

const markers = [
	{ start: 100, end: 200, content: 'Important note', color: 'blue' },
];

export default function Stage3() {
	return (
		<EditableMap/>
	);
}
